export const metadata = {
  title: "Release Branch Workflow",
  slug: "release-branch-workflow",
  locale: "en",
  section: "workflows",
  author: "lance-mq",
  summary: "Explain when to cut a release branch, when to freeze features, and how to flow fixes back to main.",
  sourceUrls: [
    "https://git-scm.com/book/en/v2/Git-Branching-Branching-Workflows",
    "https://git-scm.com/docs/git-merge",
    "https://git-scm.com/docs/git-branch",
  ],
  citations: [
    {
      title: "Git Branching Branching Workflows",
      url: "https://git-scm.com/book/en/v2/Git-Branching-Branching-Workflows",
      kind: "book",
    },
    {
      title: "Git merge",
      url: "https://git-scm.com/docs/git-merge",
      kind: "official",
    },
    {
      title: "Git branch",
      url: "https://git-scm.com/docs/git-branch",
      kind: "official",
    },
  ],
};

# Release Branch Workflow

## What you will learn

- Understand the core purpose of Release Branch Workflow
- Master the basic usage and common options of Release Branch Workflow
- Explain when to cut a release branch, when to freeze features, and how to flow fixes back to main.
- Understand key concepts: Where this workflow fits
- Know when to use this feature and when to avoid it



## Start with a problem

Your team is collaborating on a project, branches are growing, merges are becoming more frequent — but there's no stable collaboration rhythm. Everyone syncs code their own way, and conflicts are piling up.

## Where this workflow fits

<GitflowReleaseFigure
  title="Release Branch Workflow"
  caption="Cut release branch from develop for feature freeze and bug fix. After fixes, merge to main for release and backmerge to develop."
  startLabel="Start release cycle"
  releasePrepLabel="Feature freeze + Bug fixes"
  releaseBackmergeLabel="Merge to main + develop"
  hotfixBackmergeLabel="Hotfix bidirectional merge"
/>


Release branches are useful when the team needs a stable lane for test, packaging, and release-only fixes while main keeps moving.

## Suggested sequence

### 1. cut a release branch from a stable mainline

```bash
git switch main
git pull --ff-only
git switch -c release/2.4.0
```

### 2. freeze features and accept only release-related fixes

That usually means bug fixes, config adjustments, docs, and packaging changes only.

### 3. merge the final release fixes back into main

```bash
git switch main
git merge release/2.4.0
```

## Why this order is safer

Release work becomes easier when "prepare this version" is separated from "continue building new work."

## Common mistakes

- continuing to add features after cutting the release branch
- fixing release-only issues without flowing them back to main
- leaving release branches alive far beyond the release window

## Try it yourself

1. Practice the release-branch-workflow command in a test repository and observe state changes before and after
2. Experiment with different options and compare the output differences
3. Simulate a real scenario where you would need to use this, and walk through the full process

{/* internal-links:auto */}

## Further reading

Keep going on the same topic:

- [Prepare commit history before opening a pull request](/en/workflows/prepare-commits-before-pull-request)
- [Release Train Workflow](/en/workflows/release-train-workflow)
- [Rerere for Recurring Conflicts](/en/workflows/rerere-for-recurring-conflicts)

