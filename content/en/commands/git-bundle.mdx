export const metadata = {
  title: "git-bundle Tutorial",
  slug: "git-bundle",
  locale: "en",
  section: "commands",
  summary: "Explains how to use git-bundle to package repository history into an offline-transfer bundle.",
  sourceUrls: [
    "https://git-scm.com/docs/git-bundle",
  ],
};

# git-bundle Tutorial

## The short version

git-bundle is used to package repository history into an offline-transfer bundle.

## When it is a good fit

- when you need to package repository history into an offline-transfer bundle
- when you want this step to be repeatable instead of ad hoc
- when you need a clearer mental model of what Git is recording or updating

## Basic example

```bash
git bundle create repo.bundle --all
```

## What to watch most closely

These commands often change repository layout or collaboration patterns, so align on conventions first.

## A safer working habit

Test the flow in a small sandbox repository before rolling it into the main repo or automation.

## Useful angles for understanding it

- Distribute code or history
- Reduce clone or checkout scope
- Coordinate multi-repo or multi-worktree setups

## Related reading

Read it alongside git status, git log, and git show so it is easier to see how the command changes history, refs, the index, or the working tree.


## What problem this command solves in a workflow

`git bundle` is about how your local repository exchanges information with remotes, patches, or transportable artifacts. The mental model becomes clearer once you separate download, publish, integrate, and distribute.

## Typical use cases

- Use `git bundle` when synchronizing upstream state, publishing local work, or packaging history for review or transfer.
- Place `git bundle` inside collaboration flow so the team can control when information is fetched, integrated, or exposed.
- Use `git bundle` in automation, mirroring, offline transfer, or email-based workflows when Git history needs to move between systems.

## Diagram view

<CommandFlowFigure
  title="Local-to-external information flow"
  caption="Remote and patch commands are easiest to reason about when you separate where data comes from, where it goes, and whether the current branch changes as a side effect."
  inputsLabel="Sources"
  inputs="Local refs|Remote refs|Patches / artifacts"
  commandLabel="git bundle"
  outputsLabel="Results"
  outputs="Updated tracking refs|Published commits|Transfer artifacts"
  note="Any time a shared branch is involved, confirm the target ref and team policy before running the action that publishes or integrates history."
/>

## Special cases and boundaries

- A common remote-workflow mistake is treating download, integration, and publication as the same action. Split them first, then choose flags.
- If the result may affect shared branches or externally visible history, double-check the remote, the target ref, and the permission boundary before executing `git bundle`.
- Looking at `git fetch`, `git push`, and `git remote -v` together usually makes it easier to tell whether the problem is transport, auth, or ref selection.
- Bundles work well for offline history transfer, but the receiving side still needs to know which refs and prerequisite objects are required.

