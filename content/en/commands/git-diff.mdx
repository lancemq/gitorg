export const metadata = {
  title: "git diff Tutorial",
  slug: "git-diff",
  locale: "en",
  section: "commands",
  summary: "Explains how git diff compares working tree, index, and commit states, and which forms are most useful day to day.",
  sourceUrls: [
    "https://git-scm.com/docs/git-diff",
    "https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository",
  ],
};

# git diff Tutorial

## The short version

`git diff` compares two states, whether that means the working tree, the staging area, or commits in history.

## Most common forms

```bash
git diff
git diff --staged
git diff HEAD
git diff main...feature/login
```


## What problem this command solves in a workflow

In the workflow, `git diff` is mostly a “inspect first, decide second” command. It usually does not rewrite history by itself; instead, it helps you confirm the current state of the working tree, index, refs, or commit objects.

## Typical use cases

- Use `git diff` before changing files or history so you have observable evidence first.
- Put `git diff` into review, debugging, and incident-analysis flow so the team can align on the same context.
- When you need to explain why the repository looks the way it does, let `git diff` surface verifiable information first.

## Diagram view

<CommandFlowFigure
  title="Where inspection commands operate"
  caption="These commands mainly read repository state and emit information. The key is knowing which layer you are observing."
  inputsLabel="Observed layers"
  inputs="Working tree|Index|Commit history|Refs"
  commandLabel="git diff"
  outputsLabel="Outputs"
  outputs="Console output|Diff context|Decision signal"
  note="If the output looks wrong, the first thing to question is usually the scope you inspected, not whether the command “worked”."
/>

## Special cases and boundaries

- Most inspection commands do not mutate history, but their output still depends on which HEAD, path, range, or ref you asked them to inspect.
- If `git diff` shows something unexpected, first verify whether you are looking at the working tree, the index, the current branch, or a historical commit.
- Combining `git diff` with `git status`, `git log`, and `git show` is usually safer than trusting a single output in isolation.

