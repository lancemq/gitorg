export const metadata = {
  title: "git-request-pull Tutorial",
  slug: "git-request-pull",
  locale: "en",
  section: "commands",
  summary: "Explains how to use git-request-pull to generate a pull request summary for maintainers.",
  sourceUrls: [
    "https://git-scm.com/docs/git-request-pull",
    "https://git-scm.com/book/en/v2/Distributed-Git-Maintaining-a-Project",
  ],
};

# git-request-pull Tutorial

## The short version

git-request-pull is used to generate a pull request summary for maintainers.

## When it is a good fit

- when you need to generate a pull request summary for maintainers
- when you want this step to be repeatable instead of ad hoc
- when you need a clearer mental model of what Git is recording or updating

## Basic example

```bash
git request-pull origin/main https://example.com/repo.git feature
```

## What to watch most closely

Patch-based workflows are sensitive to context drift, so confirm the base state and the recovery path first.

## A safer working habit

Read the patch series first and keep abort, continue, or a backup branch ready.

## Useful angles for understanding it

- Handle mailbox patches or patch files
- Exchange changes outside direct repository access
- Preserve commit boundaries instead of copying edits manually

## Related reading

Read it alongside git status, git log, and git show so it is easier to see how the command changes history, refs, the index, or the working tree.


## What problem this command solves in a workflow

`git request-pull` generates a pull request summary message for maintainers. It does not create a hosted-platform PR — instead, it produces a text summary describing what changes exist between two points in your repository, which you can send to a maintainer via email or other communication channels.

## Typical use cases

- Generate a summary for a maintainer showing what commits exist between their branch and yours: `git request-pull origin/main <url> <branch>`.
- Include the summary in an email to request that a maintainer pull your changes from a public repository.
- Document the exact range of changes being proposed, including the commit log, for review before a merge.

## Diagram view

<CommandFlowFigure
  title="Pull request summary generation"
  caption="request-pull reads commit history between two refs and generates a text summary describing the proposed changes, suitable for communication with maintainers."
  inputsLabel="Input"
  inputs="Base ref (e.g., origin/main)|Your branch|Repository URL"
  commandLabel="git request-pull"
  outputsLabel="Output"
  outputs="Text summary with commit log|Change description for maintainers"
  note="This command generates a summary message, not a hosted-platform PR. It must still fit your team's communication flow."
/>

## Special cases and boundaries

- This command creates a pull request summary message, not a hosted-platform PR — it must still be delivered through your team's communication channel.
- The URL you provide should point to a repository the maintainer can actually fetch from; otherwise the summary is not actionable.
- If the base ref does not exist in the target repository, the summary may describe changes the maintainer cannot apply cleanly.
- `request-pull` verifies that the specified commits are reachable from your repository before generating the summary, catching mismatches early.

