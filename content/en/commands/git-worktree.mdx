export const metadata = {
  title: "git-worktree Tutorial",
  slug: "git-worktree",
  locale: "en",
  section: "commands",
  summary: "Explains how to use git-worktree to manage multiple working trees for the same repository.",
  sourceUrls: [
    "https://git-scm.com/docs/git-worktree",
  ],
};

# git-worktree Tutorial

## The short version

git-worktree is used to manage multiple working trees for the same repository.

## When it is a good fit

- when you need to manage multiple working trees for the same repository
- when you want this step to be repeatable instead of ad hoc
- when you need a clearer mental model of what Git is recording or updating

## Basic example

```bash
git worktree add ../repo-hotfix hotfix
```

## What to watch most closely

These commands often change repository layout or collaboration patterns, so align on conventions first.

## A safer working habit

Test the flow in a small sandbox repository before rolling it into the main repo or automation.

## Useful angles for understanding it

- Distribute code or history
- Reduce clone or checkout scope
- Coordinate multi-repo or multi-worktree setups

## Related reading

Read it alongside git status, git log, and git show so it is easier to see how the command changes history, refs, the index, or the working tree.


## What problem this command solves in a workflow

`git worktree` is less about a single commit and more about managing working copies, directory layout, or temporary local state. It helps solve questions like “how do I parallelize local work?” and “how do I preserve or clean my current scene?”

## Typical use cases

- Use `git worktree` when juggling multiple tasks, temporarily switching context, or cleaning up local workspace state.
- Place `git worktree` inside team habits that reduce dirty-working-tree friction and improve local task switching.
- Use `git worktree` in large repos, submodule-heavy environments, or multi-worktree setups to lower checkout cost and local cognitive load.

## Diagram view

<CommandFlowFigure
  title="Workspace-oriented command scope"
  caption="These commands focus on managing local working copies rather than simply producing new commits."
  inputsLabel="Local context"
  inputs="Branches|Directories|Worktrees|Uncommitted edits"
  commandLabel="git worktree"
  outputsLabel="Results"
  outputs="Extra checkouts|Saved context|Cleaned workspace"
  note="Whenever a command may alter what is visible on disk, ask whether the current local scene still needs to be preserved."
/>

## Special cases and boundaries

- These commands often change what is physically present on disk, so think about file preservation before you think about syntax.
- If there are still active edits, decide whether they should move with you, be parked temporarily, or be removed explicitly.
- When multiple directories or nested repositories are involved, validate the scope on a small example first.
- A branch usually cannot be checked out as a writable target in multiple worktrees at the same time, so confirm branch occupancy first.

