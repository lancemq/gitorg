export const metadata = {
  title: "Recover after a wrong cherry-pick",
  slug: "recover-after-wrong-cherry-pick",
  locale: "en",
  section: "recovery",
  summary: "If the wrong commit was cherry-picked, choose reset or revert based on whether the history is already shared, then replay the correct patch sequence.",
  sourceUrls: [
    "https://git-scm.com/docs/git-cherry-pick",
    "https://git-scm.com/docs/git-reset",
    "https://git-scm.com/docs/git-revert",
  ],
  citations: [
    {
      title: "Git cherry pick",
      url: "https://git-scm.com/docs/git-cherry-pick",
      kind: "official",
    },
    {
      title: "Git reset",
      url: "https://git-scm.com/docs/git-reset",
      kind: "official",
    },
    {
      title: "Git revert",
      url: "https://git-scm.com/docs/git-revert",
      kind: "official",
    },
  ],
};

# Recover after a wrong cherry-pick

## What you will learn

- Understand the core purpose of Recover after a wrong cherry-pick
- Master the basic usage and common options of Recover after a wrong cherry-pick
- If the wrong commit was cherry-picked, choose reset or revert based on whether the history is already shared, then replay the correct patch sequence.
- Understand key concepts: Decision boundary
- Know when to use this feature and when to avoid it


The critical decision is whether the picked commit is already shared.


## Start with a problem

You just ran a Git command and the result wasn't what you expected — maybe you even lost some commits. This has happened before, and you want a reliable set of recovery techniques.

## Decision boundary

- private/local only: reset is often acceptable
- shared/public branch: prefer revert for traceable correction

<CherryPickFigure
  title="Recover After Wrong Cherry-pick"
  caption="A wrong cherry-pick can be recovered by reverting to the pre-pick state, usually via reflog or reset."
  sourceLabel="Wrong picked commit"
  targetLabel="Current branch"
  transferLabel="Rollback operation"
/>


## Locate pre-pick state

```bash
git reflog
git switch -c rescue/pre-wrong-pick HEAD@{2}
```

## Typical recovery paths

### Unshared branch

```bash
git reset --hard HEAD@{2}
```

### Shared branch

```bash
git revert <wrong-picked-commit>
```

Then cherry-pick the correct commit(s) in the intended order.

<WarningBox title="Avoid hard reset on shared branches">
  Reset on public history shifts synchronization pain to collaborators. Revert is usually safer in team contexts.
</WarningBox>

## Good follow-up reads

1. [`recover after reset`](/en/recovery/recover-after-reset)
2. [`assess force-push impact`](/en/recovery/assess-force-push-impact)
3. [`git-cherry-pick`](/en/commands/git-cherry-pick)

## Try it yourself

1. Practice the recover-after-wrong-cherry-pick command in a test repository and observe state changes before and after
2. Experiment with different options and compare the output differences
3. Simulate a real scenario where you would need to use this, and walk through the full process

{/* internal-links:auto */}

## Further reading

Keep going on the same topic:

- [Recover after an over-aggressive reset](/en/recovery/recover-after-reset)
- [Recover a deleted branch](/en/recovery/recover-deleted-branch)
- [Recovering from a corrupted repository](/en/recovery/recover-from-corrupted-repo)

