export const metadata = {
  title: "三方合并机制",
  slug: "three-way-merge-mechanics",
  locale: "zh",
  section: "internals",
  author: "lance-mq",
  summary: "理解 Git 如何基于 merge-base 做三方合并，有助于准确判断冲突来源、合并结果与冲突解决策略。",
  sourceUrls: [
    "https://git-scm.com/docs/git-merge",
    "https://git-scm.com/docs/git-merge-base",
    "https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging",
  ],
  quotes: [
    {
      text: "Git performs a three-way merge by finding the common ancestor (merge base) of the two commits and using it as the baseline to combine the two sets of changes.",
      attribution: "Pro Git, 2nd Ed., §3.2 Basic Branching and Merging",
      url: "https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging",
    },
  ],
  stats: [
    {
      value: "merge base",
      label: "the common ancestor is computed once; conflicts only arise where both sides changed the same region",
      source: "git-merge-base(1) official manual",
      url: "https://git-scm.com/docs/git-merge-base.html",
    },
  ],
  citations: [
    {
      title: "Git merge",
      url: "https://git-scm.com/docs/git-merge",
      kind: "official",
    },
    {
      title: "Git merge base",
      url: "https://git-scm.com/docs/git-merge-base",
      kind: "official",
    },
    {
      title: "Git Branching Basic Branching and Merging",
      url: "https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging",
      kind: "book",
    },
  ],
};

# 三方合并机制

## 学完这篇你会掌握什么

- 理解 三方合并机制 的核心作用和适用场景
- 掌握 三方合并机制 的基本用法和常用参数
- 理解 Git 如何基于 merge-base 做三方合并，有助于准确判断冲突来源、合并结果与冲突解决策略。
- 理解 三方输入分别是什么 相关的概念
- 掌握 为什么会冲突 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


Git merge 的核心不是“把两边拼起来”，而是用三方输入计算结果：`base`、`ours`、`theirs`。


## 先想一个问题

你每天都在用 Git 命令，但偶尔会遇到一些 '奇怪' 的行为——比如明明没改文件却被提示有改动，或者 rebase 时突然多了预期之外的冲突。你想理解 Git 底层是怎么工作的。

## 三方输入分别是什么

- `base`：两边分支共同祖先（merge-base）
- `ours`：当前分支 tip
- `theirs`：被合并分支 tip

Git 对比的是 “base→ours” 与 “base→theirs” 的改动差异。

<MergeFigure
  title="三方合并机制"
  caption="三方合并使用两个分支的顶点和它们的共同祖先，尝试自动合并非冲突部分。"
  beforeLabel="合并前"
  afterLabel="合并后"
  modeLabel="three-way merge"
/>


## 为什么会冲突

当两边都修改了同一位置且结果不可自动判定时，就会触发冲突。

## 一个常见误解

冲突不代表 Git 不知道文件结构，而是语义选择需要人工决策。

<WarningBox title="先看 base 再解冲突">
  只看 ours/theirs 容易失去上下文。理解 base 的原始状态，通常能更快判断哪侧更合理。
</WarningBox>

## 接下来建议继续看什么

1. [`merge-base and ancestry`](/zh/internals/merge-base-and-ancestry)
2. [`git-merge`](/zh/commands/git-merge)
3. [`rerere for recurring conflicts`](/zh/workflows/rerere-for-recurring-conflicts)

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [修订选择、范围表达式与提交集合](/zh/internals/revision-selection-and-ranges)
- [传输协议、协商与 pack 交换](/zh/internals/transfer-protocols-and-negotiation)
- [Tree 对象与目录快照教程](/zh/internals/tree-objects-and-snapshots)

