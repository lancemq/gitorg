export const metadata = {
  title: "用 cherry-pick 回移修复教程",
  slug: "backport-with-cherry-pick",
  locale: "zh",
  section: "workflows",
  summary: "在旧版本维护线中按提交精确回移修复，而不是整条分支整合。",
  sourceUrls: [
    "https://git-scm.com/docs/git-cherry-pick",
    "https://git-scm.com/docs/git-branch",
  ],
};

# 用 cherry-pick 回移修复教程

## 场景说明

在旧版本维护线中按提交精确回移修复，而不是整条分支整合。

## 推荐顺序

1. 先定位需要回移的提交
2. 切到维护分支执行 cherry-pick
3. 验证差异和测试结果

## 为什么这个顺序更稳

工作流的价值通常不在某一条命令本身，而在于它把先看什么、再做什么、出了问题怎么回退固定下来。

## 相关命令

建议结合 git fetch、git log、git diff、git merge、git rebase 或 git cherry-pick 一起理解。
