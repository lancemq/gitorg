export const metadata = {
  title: "PR 前如何整理提交历史",
  slug: "prepare-commits-before-pull-request",
  locale: "zh",
  section: "workflows",
  summary: "在发起 PR 之前，围绕同步、提交整理、风险确认和 review 友好度，建立一套稳定的准备动作。",
  sourceUrls: [
    "https://git-scm.com/docs/git-rebase",
    "https://git-scm.com/docs/git-commit",
    "https://git-scm.com/book/en/v2/Distributed-Git-Contributing-to-a-Project",
  ],
};

# PR 前如何整理提交历史

PR 前整理提交，不是为了把历史“修漂亮”，而是为了让 review 更容易。

## 推荐顺序

1. 先同步主线最新状态
2. 整理明显噪音提交
3. 再检查提交信息和变更边界
4. 最后确认有没有共享历史风险

## 一个保守流程

```bash
git fetch origin
git rebase origin/main
git log --oneline --decorate -n 10
```

如果你的团队不鼓励在这个阶段 rebase，也可以改成 merge，但无论哪种，都先保证你知道当前分支和主线的关系。

## PR 前最值得确认的 4 件事

- 是否还有明显的“修修补补”提交可以整理
- 提交标题是否能独立表达意图
- 有没有把无关改动混进来
- 这段历史是否已经被别人基于它继续工作

## 一个原则

提交历史不是写给 Git 看的，是写给未来的 review、回滚和排障看的。
