export const metadata = {
  title: "提交评审前准备专题",
  slug: "pull-request-prep",
  locale: "zh",
  section: "best-practices",
  author: "lance-mq",
  summary: "在发起评审前收敛提交、同步基线并清理噪音改动。",
  sourceUrls: [
    "https://git-scm.com/book/en/v2/Distributed-Git-Contributing-to-a-Project",
    "https://git-scm.com/docs/git-rebase",
  ],
  citations: [
    {
      title: "Distributed Git Contributing to a Project",
      url: "https://git-scm.com/book/en/v2/Distributed-Git-Contributing-to-a-Project",
      kind: "book",
    },
    {
      title: "Git rebase",
      url: "https://git-scm.com/docs/git-rebase",
      kind: "official",
    },
  ],
};

# 提交评审前准备专题

## 学完这篇你会掌握什么

- 理解 提交评审前准备专题 的核心作用和适用场景
- 掌握 提交评审前准备专题 的基本用法和常用参数
- 在发起评审前收敛提交、同步基线并清理噪音改动。
- 理解 为什么"准备好再送审"会直接提升 review 质量 相关的概念
- 掌握 1. 先同步基线，再谈送审 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用



## 先想一个问题

你的团队在 Git 使用上积累了一些习惯，但你不确定这些习惯是否最优——或者你刚经历过一次因为 Git 操作不当造成的事故，想知道怎么避免下次再犯。

## 为什么"准备好再送审"会直接提升 review 质量

<CommandFlowFigure
  title="PR 准备流程"
  caption="发起 PR 前的检查清单：同步主线、整理提交、运行测试、检查 diff、编写描述。准备充分的 PR 评审更快、质量更高。"
  inputsLabel="开发完成"
  inputs="功能代码已编写|本地测试已通过|文档已更新"
  commandLabel="PR 准备"
  outputsLabel="高质量 PR"
  outputs="✅ 已 rebase 最新主线|✅ 提交已整理|✅ 测试全部通过|✅ diff 干净|✅ 描述完整"
  note="一个准备充分的 PR 是对评审者的尊重。减少来回修改，提高合并效率。"
/>

很多开发者写完代码就立刻
评审前准备的目标不是“美化历史”，而是让 reviewer 一上来就能讨论真正重要的问题。

## 1. 先同步基线，再谈送审

如果你的分支已经明显落后主线，review 成本通常会更高，因为 reviewer 很难判断：

- 你改了什么
- 主线最近又改了什么
- 冲突风险在哪里

更稳的顺序通常是：

```bash
git fetch origin
git rebase origin/main
```

或者在共享历史场景下改用 merge。重点不是必须用哪一种，而是评审前先让基线清楚。

## 2. 清理噪声提交，减少 reviewer 的负担

送审前很值得检查一下是否存在这些噪声：

- `fix typo`
- `try again`
- `temp debug`
- “第一次失败后补一下”的小修小补

如果这些提交本质属于同一个逻辑块，评审前就适合本地整理。  
这样 reviewer 看到的是更清晰的提交叙事，而不是你的整个试错过程。

## 3. 评审前最实用的三步检查

```bash
git status
git log --oneline --decorate -8
git diff --stat origin/main...
```

这三步通常足够回答：

- 工作区是不是干净的
- 最近这几条历史是不是你真的想送审的内容
- 改动规模有没有超出预期

如果你怀疑范围不对，再补：

```bash
git diff --name-only origin/main...
```

## 4. 让 reviewer 一眼看懂意图

评审前准备的另一个重点，是让历史本身可以帮助 reviewer：

- 提交顺序能讲出完整故事
- 提交标题能表达动作
- 变更边界不要东一块西一块

reviewer 理想上应该把精力放在“方案是否合理”，而不是“你到底改了哪几件事”。

## 5. 一个可执行的送审前最低标准

如果团队想先建立最轻量的共识，可以用这 5 条：

1. 先同步主线
2. 清理明显噪声提交
3. 提交标题可读
4. 用 `log` 和 `diff` 再过一遍
5. 不把无关改动一起送审

做到这一步，review 的节奏和质量通常就会明显改善。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [Git 别名与效率提升](/zh/best-practices/git-aliases)
- [Pull Request 评审准备与协作规范](/zh/best-practices/pull-request-review-readiness)
- [发布清单纪律](/zh/best-practices/release-checklist-discipline)

