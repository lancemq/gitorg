export const metadata = {
  title: "提交评审前准备专题",
  slug: "pull-request-prep",
  locale: "zh",
  section: "best-practices",
  summary: "在发起评审前收敛提交、同步基线并清理噪音改动。",
  sourceUrls: [
    "https://git-scm.com/book/en/v2/Distributed-Git-Contributing-to-a-Project",
    "https://git-scm.com/docs/git-rebase",
  ],
};

# 提交评审前准备专题

## 核心建议

在发起评审前收敛提交、同步基线并清理噪音改动。

## 为什么这件事重要

好的 Git 实践不是让命令看起来更熟练，而是让历史更可读、协作更稳定、恢复路径更明确。

## 执行清单

- 同步主分支后再送审
- 确保提交说明可读
- 用 log 和 diff 再看一遍历史

## 适合一起看的命令

通常建议同时回看 git status、git log、git diff，必要时再补上 git rebase、git merge 或 force-with-lease 相关知识。
