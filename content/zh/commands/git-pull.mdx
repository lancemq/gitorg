export const metadata = {
  title: "git pull 教程",
  slug: "git-pull",
  locale: "zh",
  section: "commands",
  summary: "解释 git pull 是 fetch 加整合的组合命令，以及为何很多团队更推荐 fetch-first 或 ff-only 的保守策略。",
  sourceUrls: [
    "https://git-scm.com/docs/git-pull",
    "https://git-scm.com/book/en/v2/Git-Branching-Remote-Branches",
  ],
};

# git pull 教程

## 一句话理解

`git pull` 会先抓取远端更新，再把它们整合进当前分支。

## 为什么它容易让人误解

因为它把“同步信息”和“修改当前分支”合在了一起，所以很多人会忽略整合策略本身。

## 常见写法

```bash
git pull
git pull --rebase
git pull --ff-only
```

## 一个更稳的习惯

如果你不想把判断交给默认行为，先 `fetch` 再决定 merge 还是 rebase，会更可控。

## 什么时候用 ff-only

当你希望分叉时直接失败，而不是隐式产生 merge commit，`--ff-only` 是很好的保守策略。


## 这条命令在流程里解决什么问题

`git pull` 围绕“本地仓库怎样与远端、补丁或可传输工件交换信息”展开。理解它时，要先分清下载、发布、整合、分发这几种动作分别由谁负责。

## 典型用例

- 在同步上游、发布本地提交或传递补丁时，用 `git pull` 控制信息流动的方向。
- 把 `git pull` 放进协作流程中，让“先观察远端，再决定整合方式”这件事更可控。
- 在自动化、镜像、离线传输或邮件流中，用 `git pull` 管理可交换的历史单元。

## 图例理解

<CommandFlowFigure
  title="本地与外部信息流"
  caption="远端与补丁类命令最关键的是分清信息从哪里来、到哪里去，以及是否会顺手改当前分支。"
  inputsLabel="来源"
  inputs="本地引用|远端引用|补丁 / 工件"
  commandLabel="git pull"
  outputsLabel="结果"
  outputs="更新后的跟踪引用|发布的提交|传输产物"
  note="只要涉及共享分支，就优先确认目标引用和团队约定，再执行真正的发布或整合动作。"
/>

## 特殊情况与边界

- 远端类命令最常见的误区，是把“下载”“整合”“发布”混成同一件事。先拆清动作，再决定参数。
- 如果操作结果会影响共享分支或对外发布，执行 `git pull` 前最好再确认一次目标远端、目标引用和权限边界。
- 和 `git fetch`、`git push`、`git remote -v` 一起看，通常更容易判断问题出在同步、认证还是引用选择。
- `git pull` 的结果会受到 `pull.rebase`、`pull.ff` 和命令参数影响，所以团队里最好统一默认策略。

