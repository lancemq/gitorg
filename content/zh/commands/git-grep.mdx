export const metadata = {
  title: "git-grep 教程",
  slug: "git-grep",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-grep 在仓库内容中搜索文本。",
  sourceUrls: [
    "https://git-scm.com/docs/git-grep",
    "https://git-scm.com/book/en/v2/Git-Tools-Searching",
  ],
  citations: [
    {
      title: "Git grep",
      url: "https://git-scm.com/docs/git-grep",
      kind: "official",
    },
    {
      title: "Git Tools Searching",
      url: "https://git-scm.com/book/en/v2/Git-Tools-Searching",
      kind: "book",
    },
  ],
};

# git-grep 教程

## 学完这篇你会掌握什么

- 理解 git-grep 教程 的核心作用和适用场景
- 掌握 git-grep 教程 的基本用法和常用参数
- 解释如何用 git-grep 在仓库内容中搜索文本。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-grep 用于在仓库内容中搜索文本。

## 什么时候适合用

- 当你需要在仓库内容中搜索文本
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git grep "TODO"
```

## 读这条命令时最该注意什么

先把默认行为弄清楚，再去组合更多参数，通常能少踩很多隐藏规则。

## 一个更稳的实践建议

把它和 status、log、diff 一起看，更容易确认到底改了什么。

## 补充理解角度

- 理解这条命令的默认行为
- 把它放进日常提交或排查流程
- 在脚本或团队习惯里稳定复用


## 这条命令在流程里解决什么问题

`git grep` 在仓库的已跟踪文件中搜索匹配的文本模式，默认忽略未跟踪文件和 .gitignore 的内容。理解它时，要把它放在"如何在代码库中快速定位特定内容"这个场景中来看。

## 典型用例

- 在代码库中快速查找某个函数、变量或常量的所有引用位置。
- 配合 `--cached` 搜索暂存区内容，或指定特定提交/分支来搜索历史版本。
- 用 `-n`（行号）、`-i`（忽略大小写）、`-c`（计数）等选项定制搜索结果。

## 图例理解

<CommandFlowFigure
  title="仓库内文本搜索"
  caption="grep 命令在已跟踪文件中执行文本模式匹配，比通用 grep 更快且结果更干净。"
  inputsLabel="输入"
  inputs="搜索模式|目标范围（工作区/暂存区/特定提交）"
  commandLabel="git grep"
  outputsLabel="结果"
  outputs="匹配行及文件位置"
  note="git grep 只搜索已跟踪文件，结果不会混入构建产物或临时文件。"
/>

## 特殊情况与边界

- `git grep` 默认只搜索已跟踪文件，.gitignore 中的文件不会被搜索到。
- 可以使用 `--and`、`--or`、`--not` 组合多个搜索模式，实现更精确的匹配。
- 搜索范围可以通过指定提交、树对象或路径来限制，比如 `git grep "TODO" v1.0 -- src/`。
- 在大型仓库中，git grep 通常比系统 grep 更快，因为 Git 只遍历索引中的文件而不是扫描整个文件系统。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [git-gc 教程](/zh/commands/git-gc)
- [git-hash-object 教程](/zh/commands/git-hash-object)
- [git-help 教程](/zh/commands/git-help)

