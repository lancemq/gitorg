export const metadata = {
  title: "git-show-ref 教程",
  slug: "git-show-ref",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-show-ref 列出仓库中的引用及其目标。",
  sourceUrls: [
    "https://git-scm.com/docs/git-show-ref",
  ],
  citations: [
    {
      title: "Git show ref",
      url: "https://git-scm.com/docs/git-show-ref",
      kind: "official",
    },
  ],
};

# git-show-ref 教程

## 学完这篇你会掌握什么

- 理解 git-show-ref 教程 的核心作用和适用场景
- 掌握 git-show-ref 教程 的基本用法和常用参数
- 解释如何用 git-show-ref 列出仓库中的引用及其目标。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-show-ref 用于列出仓库中的引用及其目标。

## 什么时候适合用

- 当你需要列出仓库中的引用及其目标
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git show-ref --heads --tags
```

## 读这条命令时最该注意什么

先把默认行为弄清楚，再去组合更多参数，通常能少踩很多隐藏规则。

## 一个更稳的实践建议

把它和 status、log、diff 一起看，更容易确认到底改了什么。

## 补充理解角度

- 理解这条命令的默认行为
- 把它放进日常提交或排查流程
- 在脚本或团队习惯里稳定复用


## 这条命令在流程里解决什么问题

`git show-ref` 解决的是"需要查看仓库中所有引用及其目标 commit hash"的问题。它列出本地仓库中所有的分支、标签和远程跟踪引用，以及它们各自指向的提交哈希值。

## 典型用例

- 排查分支指向问题时，用 `git show-ref --heads` 列出所有本地分支及其目标 commit，确认分支是否指向预期提交。
- 用 `git show-ref --tags` 查看所有标签及其对应的 commit hash，验证标签版本是否正确。
- 在脚本中通过 `git show-ref --verify refs/heads/main` 检查某个引用是否存在，并根据退出码做条件判断。

## 图例理解

<CommandFlowFigure
  title="引用的展示与验证"
  caption="show-ref 读取 .git/refs 和 packed-refs 中的所有引用，输出引用名称和目标 hash 的对应关系。"
  inputsLabel="仓库引用"
  inputs="本地分支|标签|远程跟踪引用|packed-refs"
  commandLabel="git show-ref"
  outputsLabel="产出"
  outputs="引用名称|目标 commit hash|引用存在性验证"
  note="show-ref 只显示本地仓库中的引用，不会访问远程服务器获取最新的远程引用信息。"
/>

## 特殊情况与边界

- `git show-ref` 只读取本地仓库中的引用（`.git/refs` 和 `packed-refs`），不会访问远程服务器获取最新的远程引用。
- 它是纯只读命令，不修改仓库任何内容。
- 用 `--verify` 模式可以检查特定引用是否存在，引用不存在时返回非零退出码，适合脚本中的条件判断。
- HEAD 本身不是一个普通引用，`git show-ref` 默认不显示 HEAD，需要用 `--head` 参数来包含它。
- 如果引用被打包在 `packed-refs` 文件中（常见于大量引用的仓库），`show-ref` 同样能正确读取。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

