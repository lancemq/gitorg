export const metadata = {
  title: "git push 教程",
  slug: "git-push",
  locale: "zh",
  section: "commands",
  summary: "说明 git push 如何发布本地分支、设置上游分支，以及 force-with-lease 为什么比裸 force 更安全。",
  sourceUrls: [
    "https://git-scm.com/docs/git-push",
    "https://git-scm.com/book/en/v2/Git-Branching-Remote-Branches",
  ],
};

# git push 教程

## 一句话理解

`git push` 会把本地引用和对象发布到远端，让其他协作者可以看到你的分支更新。

## 基本用法

```bash
git push origin main
git push -u origin feature/login
```

## `-u` 的作用

它会把当前本地分支与远端上游分支关联起来，后续再 push / pull 会更方便。

## 强推时为什么更推荐 `--force-with-lease`

因为它会先检查远端是否仍然处于你预期的状态，能减少误覆盖别人新提交的风险。

## 一个实践建议

共享分支默认不要随便强推；如果确实要改写，优先用 `--force-with-lease`。


## 这条命令在流程里解决什么问题

`git push` 围绕“本地仓库怎样与远端、补丁或可传输工件交换信息”展开。理解它时，要先分清下载、发布、整合、分发这几种动作分别由谁负责。

## 典型用例

- 在同步上游、发布本地提交或传递补丁时，用 `git push` 控制信息流动的方向。
- 把 `git push` 放进协作流程中，让“先观察远端，再决定整合方式”这件事更可控。
- 在自动化、镜像、离线传输或邮件流中，用 `git push` 管理可交换的历史单元。

## 图例理解

<CommandFlowFigure
  title="本地与外部信息流"
  caption="远端与补丁类命令最关键的是分清信息从哪里来、到哪里去，以及是否会顺手改当前分支。"
  inputsLabel="来源"
  inputs="本地引用|远端引用|补丁 / 工件"
  commandLabel="git push"
  outputsLabel="结果"
  outputs="更新后的跟踪引用|发布的提交|传输产物"
  note="只要涉及共享分支，就优先确认目标引用和团队约定，再执行真正的发布或整合动作。"
/>

## 特殊情况与边界

- 远端类命令最常见的误区，是把“下载”“整合”“发布”混成同一件事。先拆清动作，再决定参数。
- 如果操作结果会影响共享分支或对外发布，执行 `git push` 前最好再确认一次目标远端、目标引用和权限边界。
- 和 `git fetch`、`git push`、`git remote -v` 一起看，通常更容易判断问题出在同步、认证还是引用选择。
- 如果需要改写已经发布到远端的历史，优先用 `--force-with-lease` 而不是裸 `--force`。

