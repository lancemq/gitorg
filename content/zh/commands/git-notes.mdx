export const metadata = {
  title: "git-notes 教程",
  slug: "git-notes",
  locale: "zh",
  section: "commands",
  summary: "在不改写提交对象的前提下给提交补充审计、复盘或评审注释，适合需要保留原始历史又要追加上下文的团队。",
  sourceUrls: [
    "https://git-scm.com/docs/git-notes",
  ],
};

# git-notes 教程

## 一句话理解

`git notes` 可以给已有提交追加“旁路注释”，不会改变提交 ID。

## 什么时候适合用

- 需要记录审计结论、事故编号、上线追踪信息
- 不希望用 amend/rebase 改写历史
- 想给特定提交补后续说明而不干扰主流程提交信息

## 基本操作

```bash
git notes add -m "Incident: INC-2026-0421 mitigated"
git notes show <commit>
git log --show-notes
```

## 团队使用前要先约定

1. notes 用于什么场景（审计/复盘/合规）
2. notes ref 是否统一（默认是 `refs/notes/commits`）
3. notes 是否需要推送共享

## 共享 notes 的关键点

notes 不是默认跟着普通 `git push` 走。需要显式推送：

```bash
git push origin refs/notes/* 
```

拉取也要显式配置或执行。

## 图例理解

<CommandFlowFigure
  title="提交对象与注释分离"
  caption="notes 存放在独立引用中，提交对象本身不变，因此适合追加上下文而不重写历史。"
  inputsLabel="输入"
  inputs="既有提交|notes ref"
  commandLabel="git notes"
  outputsLabel="输出"
  outputs="附加注释|不变的 commit ID|可共享的 notes 引用"
  note="它改变的是注释引用，而不是提交对象。"
/>

## 常见误区

### 误区 1：以为 notes 所有人都能自动看到

如果没有共享 notes ref，别人看不到这些信息。

### 误区 2：把 notes 当成提交信息替代品

提交 message 仍是主叙事；notes 更适合后续追加信息。

### 误区 3：没有治理规则就大量写 notes

会形成噪声，降低检索价值。

## 接下来建议继续看什么

1. `commit message conventions`
2. `release checklist discipline`
3. `git-show`
