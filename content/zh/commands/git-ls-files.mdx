export const metadata = {
  title: "git-ls-files 教程",
  slug: "git-ls-files",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-ls-files 列出索引和工作区里的受跟踪路径。",
  sourceUrls: [
    "https://git-scm.com/docs/git-ls-files",
  ],
  citations: [
    {
      title: "Git ls files",
      url: "https://git-scm.com/docs/git-ls-files",
      kind: "official",
    },
  ],
};

# git-ls-files 教程

## 学完这篇你会掌握什么

- 理解 git-ls-files 教程 的核心作用和适用场景
- 掌握 git-ls-files 教程 的基本用法和常用参数
- 解释如何用 git-ls-files 列出索引和工作区里的受跟踪路径。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-ls-files 用于列出索引和工作区里的受跟踪路径。

## 什么时候适合用

- 当你需要列出索引和工作区里的受跟踪路径
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git ls-files
```

## 读这条命令时最该注意什么

底层命令更接近实现细节，读写前要先确认自己是不是只需要一个更安全的高层命令。

## 一个更稳的实践建议

先把它当作只读检查工具理解，再决定是否进入修改引用或索引的用法。

## 补充理解角度

- 理解底层对象和引用
- 写脚本或排查高级问题
- 验证 Git 内部状态


## 这条命令在流程里解决什么问题

`git ls-files` 解决的是"需要查看索引中有哪些文件"的问题。它列出 Git 索引（暂存区）中所有已跟踪的文件路径，是了解仓库文件跟踪状态的快速方式。

## 典型用例

- 想快速查看仓库中所有被 Git 跟踪的文件列表，直接运行 `git ls-files`，比 `git status` 更简洁。
- 在脚本中遍历所有已跟踪文件进行处理，用 `git ls-files` 输出文件列表供后续命令使用。
- 用 `git ls-files --others --ignored` 查看被 `.gitignore` 忽略的文件，排查忽略规则是否生效。

## 图例理解

<CommandFlowFigure
  title="列出索引中的文件"
  caption="ls-files 读取索引中的文件条目，输出文件路径列表。它是纯只读操作，不会修改索引或工作区。"
  inputsLabel="索引内容"
  inputs="索引中的文件条目|跟踪状态|忽略规则"
  commandLabel="git ls-files"
  outputsLabel="产出"
  outputs="已跟踪文件列表|未跟踪文件|被忽略文件|暂存状态"
  note="ls-files 只读取索引信息，不修改任何内容。它是了解仓库文件跟踪状态的纯查询工具。"
/>

## 特殊情况与边界

- `git ls-files` 是纯只读命令，不会修改索引、工作区或仓库的任何内容。
- 默认只显示已跟踪的文件，需要用 `--others` 才能显示未跟踪文件，用 `--deleted` 显示已删除但仍在索引中的文件。
- 输出的是相对于仓库根目录的路径，不受当前工作目录影响。
- 与 `git status` 不同，`ls-files` 不会比较工作区和索引的差异，它只展示索引中的内容。
- 在脚本中使用时，建议加 `-z` 以 null 字符分隔输出，避免文件名中包含空格或特殊字符导致解析错误。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

