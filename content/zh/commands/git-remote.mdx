export const metadata = {
  title: "git remote 教程",
  slug: "git-remote",
  locale: "zh",
  section: "commands",
  summary: "讲清 git remote 如何查看、添加、修改和删除远端仓库定义，以及 origin 在协作中的典型角色。",
  sourceUrls: [
    "https://git-scm.com/docs/git-remote",
    "https://git-scm.com/book/en/v2/Git-Branching-Remote-Branches",
  ],
};

# git remote 教程

## 学完这篇你会掌握什么

- 理解 git remote 教程 的核心作用和适用场景
- 掌握 git remote 教程 的基本用法和常用参数
- 讲清 git remote 如何查看、添加、修改和删除远端仓库定义，以及 origin 在协作中的典型角色。
- 理解 常见用法 相关的概念
- 掌握 origin 一定存在吗 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

`git remote` 用来管理远端仓库定义，比如查看已有远端、添加新远端或修改 URL。

## 常见用法

```bash
git remote -v
git remote add origin https://example.com/repo.git
git remote rename origin upstream
git remote remove upstream
```

## origin 一定存在吗

不是。`origin` 只是最常见的默认名字，不是 Git 的强制规则。

## 一个常见场景

在 fork 工作流里，本地往往同时配置：

- `origin`：你自己的 fork
- `upstream`：原始上游仓库


## 这条命令在流程里解决什么问题

`git remote` 围绕“本地仓库怎样与远端、补丁或可传输工件交换信息”展开。理解它时，要先分清下载、发布、整合、分发这几种动作分别由谁负责。

## 典型用例

- 在同步上游、发布本地提交或传递补丁时，用 `git remote` 控制信息流动的方向。
- 把 `git remote` 放进协作流程中，让“先观察远端，再决定整合方式”这件事更可控。
- 在自动化、镜像、离线传输或邮件流中，用 `git remote` 管理可交换的历史单元。

## 图例理解

<CommandFlowFigure
  title="本地与外部信息流"
  caption="远端与补丁类命令最关键的是分清信息从哪里来、到哪里去，以及是否会顺手改当前分支。"
  inputsLabel="来源"
  inputs="本地引用|远端引用|补丁 / 工件"
  commandLabel="git remote"
  outputsLabel="结果"
  outputs="远端列表|添加/修改/删除结果"
  note="remote 管理的是本地仓库中记录的远端定义，不会直接与远端服务器通信。"
/>

## 特殊情况与边界

- 远端类命令最常见的误区，是把“下载”“整合”“发布”混成同一件事。先拆清动作，再决定参数。
- 如果操作结果会影响共享分支或对外发布，执行 `git remote` 前最好再确认一次目标远端、目标引用和权限边界。
- 和 `git fetch`、`git push`、`git branch -vv` 一起看，通常更容易判断问题出在同步、认证还是引用选择。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

