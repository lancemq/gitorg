export const metadata = {
  title: "git status 教程",
  slug: "git-status",
  locale: "zh",
  section: "commands",
  author: "lance-mq",
  summary: "讲清 git status 如何查看工作区、暂存区和分支状态，以及它为什么是日常最应该频繁使用的命令之一。",
  sourceUrls: [
    "https://git-scm.com/docs/git-status",
    "https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository",
  ],
  citations: [
    {
      title: "Git status",
      url: "https://git-scm.com/docs/git-status",
      kind: "official",
    },
    {
      title: "Git Basics Recording Changes to the Repository",
      url: "https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository",
      kind: "book",
    },
  ],
};

# git status 教程

## 学完这篇你会掌握什么

- 理解 git status 教程 的核心作用和适用场景
- 掌握 git status 教程 的基本用法和常用参数
- 讲清 git status 如何查看工作区、暂存区和分支状态，以及它为什么是日常最应该频繁使用的命令之一。
- 理解 为什么它很重要 相关的概念
- 掌握 最常见输出内容 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

`git status` 是你了解当前仓库状态的第一入口，用来查看哪些文件被修改、哪些已经暂存、当前分支是否领先或落后。

## 为什么它很重要

很多 Git 误操作不是因为命令不会，而是因为操作前没有先看一眼当前状态。

## 最常见输出内容

- 当前分支名
- 是否有未暂存改动
- 是否有已暂存改动
- 是否存在未跟踪文件
- 是否和上游分支有差异

## 基本用法

```bash
git status
```

## 紧凑模式

```bash
git status --short
```

这更适合频繁查看。

## 一个好习惯

在这些操作前先跑一次 `git status`：

- `commit`
- `pull`
- `rebase`
- `reset`

## 常见误区

### 误区 1：status 只是给新手用的

不是。它对经验丰富的开发者同样重要，因为它是最低风险但最高价值的检查命令之一。


## 这条命令在流程里解决什么问题

从流程位置看，`git status` 更像“先观察、再决定”的命令。它通常不直接改写历史，而是帮助你确认工作区、暂存区、引用或提交对象当前到底处于什么状态。

## 典型用例

- 在执行任何操作之前，先用 `git status` 确认工作区和暂存区的当前状态。
- 把 `git status` 放进代码评审、排障和复盘流程，帮助团队对齐上下文。
- 当你需要解释“现在仓库为什么会这样”时，让 `git status` 先输出可验证的信息。

## 图例理解

<CommandFlowFigure
  title="观察类命令的作用位置"
  caption="这类命令主要读取仓库状态并输出信息，重点在于知道自己观察的是哪一层。"
  inputsLabel="观察对象"
  inputs="工作区|暂存区|提交历史|引用"
  commandLabel="git status"
  outputsLabel="产出"
  outputs="控制台信息|差异上下文|决策依据"
  note="如果输出看起来“不对”，优先排查的是观察范围，而不是命令本身有没有生效。"
/>

## 特殊情况与边界

- 大多数观察命令本身不改历史，但它看到的结果会受 HEAD、路径、提交范围或引用选择影响。
- 如果 `git status` 的输出和预期不一致，先确认你看的到底是工作区、索引、当前分支，还是某个历史提交。
- 把 `git status` 和 `git diff`、`git log`、`git show` 组合使用，通常比单独看一个输出更稳。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [git stash 教程](/zh/commands/git-stash)
- [git-submodule 教程](/zh/commands/git-submodule)
- [git switch 教程](/zh/commands/git-switch)

