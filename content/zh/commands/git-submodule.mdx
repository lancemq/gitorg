export const metadata = {
  title: "git-submodule 教程",
  slug: "git-submodule",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-submodule 管理嵌入仓库中的子模块依赖。",
  sourceUrls: [
    "https://git-scm.com/docs/git-submodule",
    "https://git-scm.com/book/en/v2/Git-Tools-Submodules",
  ],
};

# git-submodule 教程

## 一句话理解

git-submodule 用于管理嵌入仓库中的子模块依赖。

## 什么时候适合用

- 当你需要管理嵌入仓库中的子模块依赖
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git submodule update --init --recursive
```

## 读这条命令时最该注意什么

这类命令通常会改变仓库布局或协作方式，团队内最好先统一约定。

## 一个更稳的实践建议

先用小范围实验仓库验证流程，再推广到主仓库或自动化。

## 补充理解角度

- 分发代码或历史
- 降低克隆和检出范围
- 在多仓库或多工作树场景下协作


## 这条命令在流程里解决什么问题

`git submodule` 解决的是"一个仓库需要嵌入并跟踪另一个仓库的特定版本"的问题。它允许父仓库引用子仓库的特定提交，确保依赖的版本被精确锁定。

## 典型用例

- 项目依赖一个第三方库，但不想通过包管理器安装，而是直接嵌入源码并用 submodule 跟踪其特定提交。
- 在 monorepo 拆分为多仓库后，用 submodule 让主项目引用各个子仓库的稳定版本。
- 克隆包含 submodule 的仓库后，运行 `git submodule update --init --recursive` 初始化并检出所有子模块。

## 图例理解

<CommandFlowFigure
  title="子模块的依赖跟踪"
  caption="submodule 不是复制代码，而是在父仓库中记录子仓库的特定 commit hash，确保依赖版本可复现。"
  inputsLabel="父仓库"
  inputs="父仓库提交|子仓库 URL|目标 commit hash"
  commandLabel="git submodule"
  outputsLabel="结果"
  outputs="子仓库目录|锁定的版本指针|嵌套的 .git 结构"
  note="父仓库记录的是子仓库的 commit hash，不是分支名——所以 submodule 默认不会自动跟进子仓库的新提交。"
/>

## 特殊情况与边界

- 子模块状态不是"自动跟着父仓库走"，父仓库记录的是子仓库的特定 commit hash，更新子模块后必须在父仓库中提交新的指针。
- 克隆含 submodule 的仓库时，需要额外执行 `git submodule update --init` 或 `--recursive` 才能拉取子仓库内容。
- 在子模块目录内做修改后，需要在子模块自己的仓库中提交并 push，然后在父仓库中更新子模块指针。
- submodule 和 subtree 是不同的方案：submodule 保持独立仓库，subtree 则将代码合并到父仓库历史中。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。
