export const metadata = {
  title: "git-rev-parse 教程",
  slug: "git-rev-parse",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-rev-parse 解析修订表达式和仓库路径信息。",
  sourceUrls: [
    "https://git-scm.com/docs/git-rev-parse",
    "https://git-scm.com/book/en/v2/Git-Tools-Revision-Selection",
  ],
  citations: [
    {
      title: "Git rev parse",
      url: "https://git-scm.com/docs/git-rev-parse",
      kind: "official",
    },
    {
      title: "Git Tools Revision Selection",
      url: "https://git-scm.com/book/en/v2/Git-Tools-Revision-Selection",
      kind: "book",
    },
  ],
};

# git-rev-parse 教程

## 学完这篇你会掌握什么

- 理解 git-rev-parse 教程 的核心作用和适用场景
- 掌握 git-rev-parse 教程 的基本用法和常用参数
- 解释如何用 git-rev-parse 解析修订表达式和仓库路径信息。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-rev-parse 用于解析修订表达式和仓库路径信息。

## 什么时候适合用

- 当你需要解析修订表达式和仓库路径信息
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git rev-parse --show-toplevel
```

## 读这条命令时最该注意什么

先把默认行为弄清楚，再去组合更多参数，通常能少踩很多隐藏规则。

## 一个更稳的实践建议

把它和 status、log、diff 一起看，更容易确认到底改了什么。

## 补充理解角度

- 理解这条命令的默认行为
- 把它放进日常提交或排查流程
- 在脚本或团队习惯里稳定复用


## 这条命令在流程里解决什么问题

`git rev-parse` 解决的是"需要将修订表达式（如分支名、标签、相对引用）解析为完整的 commit hash 或获取仓库路径信息"的问题。它是脚本中将人类可读的引用转换为机器可用格式的核心工具。

## 典型用例

- 在脚本中获取仓库根目录路径，用 `git rev-parse --show-toplevel` 获取绝对路径，不依赖当前工作目录。
- 将分支名或标签解析为完整 hash，用 `git rev-parse main` 或 `git rev-parse v1.0.0` 获取对应的 commit hash。
- 获取 `.git` 目录位置，用 `git rev-parse --git-dir` 在脚本中定位仓库的元数据目录。

## 图例理解

<CommandFlowFigure
  title="解析修订表达式与仓库路径"
  caption="rev-parse 将人类可读的引用（分支名、标签、相对表达式）转换为 commit hash，或输出仓库的路径信息。它是纯只读操作。"
  inputsLabel="修订表达式/路径查询"
  inputs="分支名|标签|相对引用(HEAD~1)|路径查询选项"
  commandLabel="git rev-parse"
  outputsLabel="产出"
  outputs="完整 commit hash|仓库根目录|.git 目录路径|验证结果"
  note="rev-parse 只做解析和查询，不修改仓库任何内容。它是脚本中获取仓库信息的瑞士军刀。"
/>

## 特殊情况与边界

- `git rev-parse` 是纯只读命令，不会修改仓库、索引或工作区的任何内容。
- 如果传入的修订表达式无效（如不存在的分支名），会输出错误信息并返回非零退出码。
- 不同选项的输出格式不同：有些输出单行（如 `--show-toplevel`），有些输出多行，在脚本中使用时需要针对性处理。
- `--verify` 选项可以验证引用是否存在且有效，适合在脚本中做前置检查。
- 相对引用如 `HEAD~3`、`main^{tree}` 等都能被正确解析，但需要确保引用在当前仓库中存在。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

