export const metadata = {
  title: "git checkout 教程",
  slug: "git-checkout",
  locale: "zh",
  section: "commands",
  summary: "说明 git checkout 作为旧式多用途命令的两大职责，以及它与 switch、restore 的现代分工。",
  sourceUrls: [
    "https://git-scm.com/docs/git-checkout",
    "https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging",
    "https://git-scm.com/book/en/v2/Git-Basics-Undoing-Things",
  ],
};

# git checkout 教程

## 一句话理解

`git checkout` 是 Git 里的经典多用途命令，既能切分支，也能把路径恢复到指定状态。

## 为什么它现在看起来有点“重”

正因为它承担了两类不同职责，所以新版本 Git 才引入了 `git switch` 和 `git restore` 来拆分语义。

## 旧式但仍然常见的两种用法

### 切换分支

```bash
git checkout main
git checkout -b feature/login
```

### 恢复路径

```bash
git checkout -- README.md
```

这会把工作区里的该文件恢复到索引版本，效果上接近今天的 `git restore README.md`。

## checkout 某个提交

```bash
git checkout <commit>
```

这样会让你进入 detached HEAD 状态，适合查看历史，但如果你继续提交并希望保留结果，就要尽快创建分支。

## 什么时候还需要理解 checkout

- 维护老项目或老脚本
- 阅读旧教程和团队历史文档
- 理解 `switch` / `restore` 是从哪里拆出来的

## checkout 和新命令的关系

- `git switch` 继承了“切分支”这部分职责
- `git restore` 继承了“恢复路径”这部分职责

所以今天你仍然应该会读懂 checkout，但在教学和日常表达上，通常优先用更明确的新命令。

## 常见风险

如果你用 checkout 恢复路径，本地改动可能被直接覆盖；如果你 checkout 到某个提交而忘记自己处在 detached HEAD，也容易在后面弄丢实验性提交。


## 这条命令在流程里解决什么问题

`git checkout` 更接近工作区、暂存区和当前分支这三层状态。理解这类命令时，最重要的不是背参数，而是先判断它会不会改动文件内容、索引快照或当前引用。

## 典型用例

- 把 `git checkout` 放进“修改 -> 检查 -> 暂存 -> 提交”的日常循环中，控制当前状态。
- 在切换上下文、整理提交或恢复文件时，用 `git checkout` 精准处理工作区与索引。
- 在团队协作前，先通过 `git checkout` 把本地状态整理干净，减少后续冲突成本。

## 图例理解

<CommandFlowFigure
  title="工作区 / 暂存区 / 引用三层关系"
  caption="状态类命令通常围绕工作区、索引和当前分支展开，关键是弄清它写到了哪一层。"
  inputsLabel="输入状态"
  inputs="工作区文件|暂存区索引|当前分支"
  commandLabel="git checkout"
  outputsLabel="可能变化"
  outputs="文件内容|暂存结果|当前引用"
  note="执行前后各跑一次 git status，通常是判断这类命令是否按预期工作的最快办法。"
/>

## 特殊情况与边界

- 这类命令往往直接碰工作区或暂存区，所以真正的风险不在“命令长什么样”，而在你是否分清它会改哪一层。
- 如果当前目录里还有未备份改动，执行 `git checkout` 前先决定这些改动是要提交、stash 还是丢弃。
- 和 `git status` 搭配读，会更容易判断 `git checkout` 执行前后到底改变了什么。
- `git checkout` 同时承担“切分支”和“还原路径”两类语义，读命令时一定要先看参数后面的对象到底是分支还是路径。

