export const metadata = {
  title: "git-rev-list 教程",
  slug: "git-rev-list",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-rev-list 以脚本友好的方式枚举提交集合。",
  sourceUrls: [
    "https://git-scm.com/docs/git-rev-list",
  ],
  citations: [
    {
      title: "Git rev list",
      url: "https://git-scm.com/docs/git-rev-list",
      kind: "official",
    },
  ],
};

# git-rev-list 教程

## 学完这篇你会掌握什么

- 理解 git-rev-list 教程 的核心作用和适用场景
- 掌握 git-rev-list 教程 的基本用法和常用参数
- 解释如何用 git-rev-list 以脚本友好的方式枚举提交集合。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-rev-list 用于以脚本友好的方式枚举提交集合。

## 什么时候适合用

- 当你需要以脚本友好的方式枚举提交集合
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git rev-list --count HEAD
```

## 读这条命令时最该注意什么

底层命令更接近实现细节，读写前要先确认自己是不是只需要一个更安全的高层命令。

## 一个更稳的实践建议

先把它当作只读检查工具理解，再决定是否进入修改引用或索引的用法。

## 补充理解角度

- 理解底层对象和引用
- 写脚本或排查高级问题
- 验证 Git 内部状态


## 这条命令在流程里解决什么问题

`git rev-list` 解决的是"需要以脚本友好的方式列出提交"的问题。它以简洁的格式输出提交 hash 列表，支持丰富的范围过滤和遍历选项，是脚本和自动化中处理提交集合的首选工具。

## 典型用例

- 统计两个版本之间的提交数量，用 `git rev-list --count v1.0..v2.0` 获取精确数字。
- 在 CI 脚本中遍历某个分支的所有提交进行处理，用 `git rev-list main` 输出提交 hash 列表。
- 查找引入某个问题的提交，用 `git rev-list --ancestry-path OLD..NEW` 限定提交范围。

## 图例理解

<CommandFlowFigure
  title="以脚本友好方式列出提交"
  caption="rev-list 读取提交历史，按指定范围输出提交 hash 列表。它是纯只读操作，不修改仓库任何内容。"
  inputsLabel="提交范围"
  inputs="起始提交|结束提交|路径过滤|遍历选项"
  commandLabel="git rev-list"
  outputsLabel="产出"
  outputs="提交 hash 列表|提交计数|首个/最新提交"
  note="rev-list 输出的是纯文本 hash 列表，非常适合在脚本中管道传递给其他命令处理。"
/>

## 特殊情况与边界

- `git rev-list` 是纯只读命令，不会修改仓库、索引或工作区的任何内容。
- 默认按时间倒序输出（最新提交在前），需要用 `--reverse` 反转顺序。
- 输出格式非常简洁（每行一个 hash），与 `git log` 不同，它不包含提交消息等人类可读的信息。
- 支持丰富的过滤选项：`--count` 只输出数量，`--max-count` 限制数量，`--after`/`--before` 按时间过滤。
- 对于非常大的仓库，输出可能非常长，建议配合 `--count` 或 `--max-count` 限制输出量。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [git restore 教程](/zh/commands/git-restore)
- [git-rev-parse 教程](/zh/commands/git-rev-parse)
- [git revert 教程](/zh/commands/git-revert)

