export const metadata = {
  title: "git-shortlog 教程",
  slug: "git-shortlog",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-shortlog 按作者和主题汇总提交历史。",
  sourceUrls: [
    "https://git-scm.com/docs/git-shortlog",
    "https://git-scm.com/book/en/v2/Distributed-Git-Maintaining-a-Project",
  ],
};

# git-shortlog 教程

## 学完这篇你会掌握什么

- 理解 git-shortlog 教程 的核心作用和适用场景
- 掌握 git-shortlog 教程 的基本用法和常用参数
- 解释如何用 git-shortlog 按作者和主题汇总提交历史。
- 理解 什么时候适合用 相关的概念
- 掌握 基本示例 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 一句话理解

git-shortlog 用于按作者和主题汇总提交历史。

## 什么时候适合用

- 当你需要按作者和主题汇总提交历史
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git shortlog -sn
```

## 读这条命令时最该注意什么

先把默认行为弄清楚，再去组合更多参数，通常能少踩很多隐藏规则。

## 一个更稳的实践建议

把它和 status、log、diff 一起看，更容易确认到底改了什么。

## 补充理解角度

- 理解这条命令的默认行为
- 把它放进日常提交或排查流程
- 在脚本或团队习惯里稳定复用


## 这条命令在流程里解决什么问题

`git shortlog` 解决的是"需要按作者汇总提交信息"的问题。它将 `git log` 的输出按作者分组并统计提交数量，适合生成发布说明、贡献者列表或统计团队工作量。

## 典型用例

- 发布新版本时，用 `git shortlog -sn v1.0..v2.0` 生成按提交数排序的贡献者列表，放入发布说明中。
- 回顾一个版本的变更时，用 `git shortlog --no-merges` 过滤掉合并提交，只看实际的代码贡献。
- 用 `git shortlog -sne` 同时显示作者姓名、邮箱和提交数，用于贡献者统计和报告。

## 图例理解

<CommandFlowFigure
  title="按作者汇总提交"
  caption="shortlog 读取提交历史，按作者分组并汇总提交主题，输出适合人类阅读的报告格式。"
  inputsLabel="提交历史"
  inputs="提交记录|作者信息|提交主题|提交范围"
  commandLabel="git shortlog"
  outputsLabel="产出"
  outputs="按作者分组|提交数量统计|汇总的提交主题"
  note="shortlog 的输出可以直接用于发布说明，但默认从 stdin 读取——不加范围参数时行为与 git log 不同。"
/>

## 特殊情况与边界

- `git shortlog` 默认从 stdin 读取（常用于管道 `git log | git shortlog`），如果直接运行且不在 tty 中可能无输出。直接运行时建议加上提交范围如 `HEAD` 或 `v1.0..v2.0`。
- 它只读取提交历史，不修改仓库任何内容，是纯只读命令。
- 用 `-s` 只显示统计数字，不加则同时显示每个提交的主题行。
- 合并提交也会被统计，如果需要排除合并提交，需要搭配 `--no-merges`（通常通过 `git log --no-merges | git shortlog` 实现）。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

