export const metadata = {
  title: "git reflog",
  slug: "git-reflog",
  locale: "zh",
  section: "commands",
  summary: "查看引用移动历史，是 reset、rebase、误删分支等恢复场景里最常用的自救命令之一。",
  sourceUrls: [
    "https://git-scm.com/docs/git-reflog",
    "https://git-scm.com/book/en/v2/Git-Internals-Git-References",
  ],
  citations: [
    {
      title: "Git reflog",
      url: "https://git-scm.com/docs/git-reflog",
      kind: "official",
    },
    {
      title: "Git Internals Git References",
      url: "https://git-scm.com/book/en/v2/Git-Internals-Git-References",
      kind: "book",
    },
  ],
};

# git reflog

## 学完这篇你会掌握什么

- 理解 git reflog 的核心作用和适用场景
- 掌握 git reflog 的基本用法和常用参数
- 查看引用移动历史，是 reset、rebase、误删分支等恢复场景里最常用的自救命令之一。
- 理解 为什么它特别重要 相关的概念
- 掌握 常见写法 相关的操作
- 知道在什么场景下使用该命令，什么场景下避免使用


`git reflog` 记录的是引用移动历史，而不是普通提交历史。很多恢复操作首先要看的不是 `git log`，而是 reflog。


## 先想一个问题

你正在 Git 仓库里工作，遇到了一个具体的操作需求——但你不确定用哪个命令、用哪个参数最合适，也不知道这个命令到底能做什么、不能做什么。

## 为什么它特别重要

当你做了这些操作后：

- `reset --hard`
- `rebase`
- `pull`
- 删除或改写分支

旧提交未必立刻消失，但你可能失去了“当前分支名”。这时 reflog 常常还能告诉你原来在哪里。

## 常见写法

```bash
git reflog
git reflog show HEAD
git reset --hard HEAD@{1}
git checkout -b rescue HEAD@{2}
```

## 实战思路

1. 先用 `git reflog` 找到误操作前的位置
2. 再决定是新建救援分支，还是直接 reset 回去

<ReflogFigure
  title="reflog 保存的是位置变动线索"
  caption="很多恢复问题里，最关键的不是旧提交不存在，而是当前分支名已经不再指向它。reflog 把这些旧位置暂时保留下来，方便你先接成救援分支。"
  historyLabel="位置历史"
  rescueLabel="恢复动作"
  nowLabel="当前 HEAD"
/>

## 和 `git log` 的区别

- `git log` 看的是提交图
- `git reflog` 看的是引用曾经指向过哪里

所以 reflog 更像“位置历史”，不是“协作历史”。

## 风险提示

reflog 不是永久保险箱。随着时间推移和垃圾回收，过旧且不可达的对象可能被清理。所以出问题后越早处理越稳。


## 这条命令在流程里解决什么问题

`git reflog` 是一个纯只读的诊断工具，不修改任何提交或引用。它记录的是引用曾经指向过哪些位置，是恢复误操作后最重要的自救工具。它不会重塑历史，但能帮你找到历史被改之前的位置。

## 典型用例

- 执行 `reset --hard` 后后悔了，用 `git reflog` 找到 reset 前的位置。
- 误删分支后，用 reflog 找到旧提交，重新建分支接住它。
- rebase 后发现历史不对，用 reflog 回到 rebase 前的状态。
- 用 `HEAD@{N}` 语法直接引用 reflog 中的旧位置，比如 `HEAD@{1}` 表示上一个 HEAD 位置。

## 图例理解

<CommandFlowFigure
  title="只读诊断命令的作用面"
  caption="reflog 不修改仓库任何状态，只输出引用移动的历史记录。"
  inputsLabel="输入"
  inputs="引用名称（HEAD 或分支名）"
  commandLabel="git reflog"
  outputsLabel="结果"
  outputs="引用移动时间线|旧位置 hash|操作描述"
  note="reflog 是纯只读操作，不会影响仓库历史。它是本地记录，不会被 push/fetch 同步。"
/>

## 特殊情况与边界

- reflog 是本地记录，不会被 push 或 fetch 同步到远端。每个人的 reflog 是独立的。
- reflog 条目有保质期：默认 unreachable 的引用保留 30 天，reachable 的保留 90 天。`git gc` 会清理过期条目。
- `git reflog expire --expire=now --all` 可以强制清理，但这是不可逆的。
- `git reflog delete` 可以删除特定条目，但通常不需要手动管理。
- 恢复操作的最佳实践：先用 `git checkout -b rescue/...` 接住旧位置，再决定是否 reset。

## 跟着做一遍

<PracticeLab
  title="练习：用 reflog 找回 reset 前的位置"
  intro="这组练习的重点不是把仓库弄乱，而是体验“位置变了但对象可能还在”的恢复思路。"
  setupLabel="准备环境"
  setup={`git switch -c lab/reflog-demo\n# 做两次提交\n# 然后执行一次 git reset --hard HEAD~1`}
  stepsLabel="跟着做"
  steps="运行 `git reflog`，找到 reset 前的 HEAD 位置。|先用 `git checkout -b rescue/reflog HEAD@{1}` 把旧位置接住。|再用 `git log --oneline --graph --decorate --all` 对照现状。"
  outcomesLabel="结果会怎样"
  outcomes="你会看到 HEAD 的移动记录仍然保留在 reflog 里。|旧提交常常并没有立刻消失，只是当前分支不再指向它。|先建救援分支通常比直接 hard reset 更稳。"
  mistakesLabel="常见错误判断"
  mistakes="如果还没确认旧位置，就不要连续多次 reset 或 rebase。|不要把 reflog 当永久保险箱，出问题后越早接住旧位置越稳。|如果恢复动作会影响共享分支，先判断影响面再改引用。"
/>

## 给你的练习

1. 在一个测试仓库中练习该命令的基本用法，观察执行前后的状态变化
2. 尝试该命令的不同参数选项，对比输出结果的差异
3. 模拟一个需要使用该命令的实际场景，完整走一遍操作流程

{/* internal-links:auto */}

## 延伸阅读

沿着同一主题继续深入：

- [git rebase 教程](/zh/commands/git-rebase)
- [git remote 教程](/zh/commands/git-remote)
- [git-replace 教程](/zh/commands/git-replace)

