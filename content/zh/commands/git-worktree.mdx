export const metadata = {
  title: "git-worktree 教程",
  slug: "git-worktree",
  locale: "zh",
  section: "commands",
  summary: "解释如何用 git-worktree 为同一仓库创建多个工作树。",
  sourceUrls: [
    "https://git-scm.com/docs/git-worktree",
  ],
};

# git-worktree 教程

## 一句话理解

git-worktree 用于为同一仓库创建多个工作树。

## 什么时候适合用

- 当你需要为同一仓库创建多个工作树
- 当你想把这类操作做成稳定流程而不是手工重复
- 当你需要更准确地理解 Git 在这一步到底记录了什么

## 基本示例

```bash
git worktree add ../repo-hotfix hotfix
```

## 读这条命令时最该注意什么

这类命令通常会改变仓库布局或协作方式，团队内最好先统一约定。

## 一个更稳的实践建议

先用小范围实验仓库验证流程，再推广到主仓库或自动化。

## 补充理解角度

- 分发代码或历史
- 降低克隆和检出范围
- 在多仓库或多工作树场景下协作


## 这条命令在流程里解决什么问题

`git worktree` 解决的是"需要在同一仓库的多个分支上并行工作"的问题。它从同一个 `.git` 目录创建多个独立的工作目录，每个工作目录可以检出不同的分支，无需反复 stash 或 clone。

## 典型用例

- 在 main 分支开发时，突然需要修复一个紧急 bug，用 `git worktree add` 创建独立的 hotfix 工作目录，两个目录并行工作互不干扰。
- 在 CI 环境中，用 worktree 同时检出多个分支进行对比测试或基准测试。
- review 同事的 PR 时，在不离开当前分支的情况下，用 worktree 检出 PR 分支进行本地验证。

## 图例理解

<CommandFlowFigure
  title="多工作树并行开发"
  caption="worktree 共享同一个 .git 目录，但每个工作目录有独立的 HEAD 和工作区，实现真正的并行开发。"
  inputsLabel="共享仓库"
  inputs="同一个 .git 目录|不同分支|独立工作目录路径"
  commandLabel="git worktree"
  outputsLabel="结果"
  outputs="多个工作目录|各自的 HEAD|并行开发环境"
  note="同一个分支通常不能同时在多个 worktree 里作为可写检出目标，规划前先确认分支占用情况。"
/>

## 特殊情况与边界

- 同一个分支通常不能同时在多个 worktree 里作为可写检出目标，规划前先确认分支占用情况。
- worktree 共享同一个 `.git` 目录，所以 fetch、pull 等操作对所有 worktree 都生效。
- 删除 worktree 目录后，需要用 `git worktree prune` 清理残留的 worktree 元数据。
- worktree 中的未提交改动属于各自独立的工作区，不会互相影响，但都存储在同一个对象数据库中。


## 延伸阅读

继续搭配 git status、git log、git show 一起看，通常更容易判断这条命令对历史、索引和工作区分别造成了什么影响。
